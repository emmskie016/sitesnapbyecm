<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>{{ copy.meta.title }}</title>
<meta name="description" content="{{ copy.meta.description }}">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --primary: {{ palette.primary }};
    --accent: {{ palette.accent }};
    --bg: {{ palette.neutral_bg }};
    --surface: {{ palette.neutral_surface }};
    --ink: {{ palette.ink }};
    --ink-soft: {{ palette.ink_soft }};
    --display: 'Playfair Display', Georgia, serif;
    --body: 'Inter', system-ui, sans-serif;
  }
  *,*::before,*::after { box-sizing: border-box; }
  html,body { margin: 0; padding: 0; }
  body { font-family: var(--body); background: var(--bg); color: var(--ink); line-height: 1.65; font-size: 17px; font-weight: 400; -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
  a { color: var(--primary); text-decoration: none; }
  .wrap { max-width: 1180px; margin: 0 auto; padding: 0 24px; }
  .hero { position: relative; min-height: 88vh; display: grid; place-items: center; color: #fff; text-align: center; overflow: hidden; border-radius: 0 0 32px 32px; }
  .hero::before { content: ''; position: absolute; inset: 0; background-image: url('{{ images.hero.local }}'); background-size: cover; background-position: center; z-index: 0; }
  .hero::after { content: ''; position: absolute; inset: 0; background: linear-gradient(180deg, color-mix(in srgb, var(--ink) 30%, transparent) 0%, color-mix(in srgb, var(--ink) 65%, transparent) 100%); z-index: 1; }
  .hero-inner { position: relative; z-index: 2; padding: 96px 24px; max-width: 780px; }
  .hero h1 { font-family: var(--display); font-weight: 400; font-size: clamp(44px, 7.2vw, 80px); line-height: 1.05; margin: 0 0 24px; letter-spacing: -.025em; }
  .hero p.lede { font-size: 20px; line-height: 1.55; opacity: .92; margin: 0 0 36px; font-weight: 400; }
  .btn { display: inline-block; padding: 16px 30px; border-radius: 999px; font-weight: 500; font-size: 15px; letter-spacing: -.005em; transition: transform .15s, box-shadow .2s; }
  .btn:hover { transform: translateY(-1px); }
  .btn-primary { background: var(--accent); color: var(--ink); }
  .btn-primary:hover { box-shadow: 0 12px 28px -10px color-mix(in srgb, var(--accent) 70%, transparent); }
  .btn-ghost-light { color: #fff; border: 1px solid rgba(255,255,255,.45); }
  .nav { position: absolute; top: 0; left: 0; right: 0; z-index: 3; display: flex; align-items: center; justify-content: space-between; padding: 28px 32px; }
  .brand { font-family: var(--display); font-size: 22px; font-weight: 500; letter-spacing: -.01em; color: #fff; }
  section.story { padding: 112px 0; text-align: center; }
  h2 { font-family: var(--display); font-weight: 400; font-size: clamp(32px, 5vw, 56px); line-height: 1.1; margin: 0 0 20px; letter-spacing: -.02em; }
  .story p.lede { color: var(--ink-soft); max-width: 38em; margin: 0 auto 64px; font-size: 19px; line-height: 1.6; }
  .gallery { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
  .gallery figure { margin: 0; aspect-ratio: 1/1; overflow: hidden; border-radius: 16px; }
  .gallery img { width: 100%; height: 100%; object-fit: cover; display: block; transition: transform .4s; }
  .gallery figure:hover img { transform: scale(1.04); }
  .feature-strip { background: var(--primary); color: var(--surface); padding: 80px 0; }
  .feature-strip .features { display: grid; grid-template-columns: repeat(3, 1fr); gap: 36px; }
  .feature { padding: 0 8px; }
  .feature h3 { font-family: var(--display); font-weight: 500; font-size: 24px; margin: 0 0 10px; letter-spacing: -.015em; }
  .feature p { margin: 0; opacity: .88; font-size: 16px; line-height: 1.6; }
  .quotes { padding: 112px 0; text-align: center; }
  .quotes blockquote { font-family: var(--display); font-weight: 400; font-size: clamp(22px, 3vw, 34px); line-height: 1.35; margin: 0 auto 20px; max-width: 32em; font-style: italic; letter-spacing: -.01em; }
  .quotes cite { color: var(--ink-soft); font-style: normal; font-size: 15px; }
  .cta { padding: 112px 0; text-align: center; background: var(--ink); color: var(--surface); }
  .cta h2 { color: var(--surface); }
  footer { padding: 56px 0; text-align: center; color: var(--ink-soft); font-size: 14px; }
  footer .attribution { margin-top: 12px; font-size: 12px; opacity: .65; }
  footer .attribution a { color: var(--ink-soft); }
  @media (max-width: 800px) {
    .gallery, .feature-strip .features { grid-template-columns: 1fr; }
    .nav { padding: 18px 20px; }
  }
</style>
</head>
<body>
<header class="hero">
  <nav class="nav">
    <div class="brand">{{ brand.name }}</div>
    <a class="btn btn-ghost-light" href="#visit">{{ copy.secondary_cta }}</a>
  </nav>
  <div class="hero-inner">
    <h1>{{ copy.headline }}</h1>
    <p class="lede">{{ copy.subheadline }}</p>
    <a class="btn btn-primary" href="#visit">{{ copy.primary_cta }}</a>
  </div>
</header>

<section class="story">
  <div class="wrap">
    <h2>{{ copy.about.heading }}</h2>
    <p class="lede">{{ copy.about.body }}</p>
    <div class="gallery">
      <figure><img src="{{ images.feature_1.local }}" alt=""></figure>
      <figure><img src="{{ images.feature_2.local }}" alt=""></figure>
      <figure><img src="{{ images.feature_3.local }}" alt=""></figure>
    </div>
  </div>
</section>

<section class="feature-strip">
  <div class="wrap">
    <div class="features">
      {% for f in copy.features %}
      <div class="feature"><h3>{{ f.title }}</h3><p>{{ f.body }}</p></div>
      {% endfor %}
    </div>
  </div>
</section>

{% if copy.social_proof %}
<section class="quotes">
  <div class="wrap">
    <blockquote>&ldquo;{{ copy.social_proof[0].text }}&rdquo;</blockquote>
    <cite>— {{ copy.social_proof[0].author }}</cite>
  </div>
</section>
{% endif %}

<section id="visit" class="cta">
  <div class="wrap">
    <h2>{{ copy.footer_tagline }}</h2>
    <p style="opacity:.85; margin-bottom:32px;">{{ copy.about.heading }}</p>
    <a class="btn btn-primary" href="mailto:hello@{{ brand.slug }}.sitesnap.app">{{ copy.primary_cta }}</a>
  </div>
</section>

<footer>
  <div>&copy; {{ now.year }} {{ brand.name }}</div>
  <div class="attribution">{{ images.hero.attribution | safe }}</div>
</footer>
</body>
</html>
